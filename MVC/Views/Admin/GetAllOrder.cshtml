@{
    ViewData["Title"] = "GetActiveOrder";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@using Entity.Concrete;
@using MVC.Models;
@model OrderVM
<h1 class="bg-info text-white">Tüm Siparişler</h1>
@if (TempData["result"] != null)
{
    <script>
        alert("İşlem başarılı");
    </script>
}
@if (TempData["resultError"] != null)
{
    <script>
        alert("İşlem başarısız");
    </script>
}



@*<a asp-action="CreateExtra" asp-controller="Admin" class="btn btn-secondary">Extra Oluştur</a>*@
<br />
<br />
<div class="container">
    <table class=" table table-sm table-bordered">
        <tr>

            <th>ID</th>
            <th>İsim</th>
            <th>Soyisim</th>
            <th>Sipariş Tarihi</th>
            <th>Kupon</th>
            <th>Aktiflik Durumu</th>
            <th>Sipariş Toplam Fiyatı</th>
            @*  <th>Güncelle</th>
            <th>Sil</th>*@


        </tr>
        @foreach (var item in Model.OrderList)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>@item.CreatedTime</td>
                <td>@item.CouponName</td>
                <td>@item.State</td>
                <td>@item.OrderTotal</td>

             @*   <td>
                    <a class="btn btn-sm btn-primary" asp-action="UpdateOrder" asp-controller="Admin" asp-route-id="@item.Id">Güncelle</a>
                </td>*@
                @*  <td>
            <form asp-action="DeleteExtra" asp-controller="Admin" asp-route-id="@item.Id" method="post">
            <button type="submit" class="btn btn-sm btn-danger">Sil</button>
            </form>
            </td>*@
            </tr>
        }

    </table>
</div>



